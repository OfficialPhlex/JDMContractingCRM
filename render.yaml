services:
  - type: web
    name: jdm-contracting-crm
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node dist/index.cjs
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
